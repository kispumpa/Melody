<Application x:Class="Melody.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Melody.UI"
             xmlns:vms="clr-namespace:Melody.UI.ViewModels"
             StartupUri="BaseWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Default.xaml"/>
            </ResourceDictionary.MergedDictionaries>


            <DataTemplate DataType="{x:Type vms:MenuWindowViewModel}">
                <local:MenuWindow />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:MainWindowViewModel}">
                <local:MainWindow />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:PracticeWindowViewModel}">
                <local:PracticeWindow />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:SettingsWindowViewModel}">
                <local:SettingsWindow />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:BaseWindowViewModel}">
                <local:BaseWindow />
            </DataTemplate>
            <Style x:Key="HomeButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="40"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Padding" Value="0"/>

                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>

                <Setter Property="Margin" Value="5"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{DynamicResource HomeButtonBorderBrush}"/>
                <Setter Property="Background" Value="{DynamicResource HomeButtonBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource HomeButtonForeground}"/>

                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Path Data="M10,20V14H14V20H19V12H22L12,3L2,12H5V20H10Z" 
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                          Width="16" Height="16" 
                          Stretch="Uniform" />
                            </Grid>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
