<UserControl x:Class="Melody.UI.SettingsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Melody.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource SettingsBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="11*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Command="{Binding GoHomeCommand}" Style="{StaticResource HomeButtonStyle}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="50,20,50,20">

                <TextBlock Text="Settings" FontSize="36" Foreground="{DynamicResource SettingsMainText}" FontWeight="Bold" Margin="0,0,0,40" />

                <Border Background="{DynamicResource SettingsBorderBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Theme" Foreground="{DynamicResource SettingsMainText}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <TextBlock Text="Choose the appearance of the application." Foreground="{DynamicResource SettingsSubText}" FontSize="14" Margin="0,0,0,15"/>

                        <ComboBox ItemsSource="{Binding AvailableThemes}" 
                                  SelectedItem="{Binding SelectedTheme}" 
                                  Width="300" 
                                  HorizontalAlignment="Left" 
                                  Padding="8" 
                                  FontSize="14"/>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource SettingsBorderBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Piano Roll Display" Foreground="{DynamicResource SettingsMainText}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <TextBlock Text="Set whether to display the full 88-key piano or only the active octaves." Foreground="{DynamicResource SettingsSubText}" FontSize="14" Margin="0,0,0,15"/>

                        <StackPanel Margin="5,0,0,0">
                            <RadioButton Content="Only active octaves (Dynamic)" 
                                IsChecked="{Binding IsDynamicPianoWidth}" 
                                GroupName="PianoWidthGroup"
                                Foreground="{DynamicResource SettingsOptionText}" FontSize="14">
                                <RadioButton.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2" />
                                </RadioButton.LayoutTransform>
                            </RadioButton>
                            <RadioButton Content="Full piano (88 keys)" 
                                IsChecked="{Binding IsFullPianoWidth}" 
                                GroupName="PianoWidthGroup"
                                Foreground="{DynamicResource SettingsOptionText}" FontSize="14" Margin="0,0,0,10">
                                <RadioButton.LayoutTransform>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2" />
                                </RadioButton.LayoutTransform>
                            </RadioButton>
                        </StackPanel>

                    </StackPanel>
                </Border>
                <Border Background="{DynamicResource SettingsBorderBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Language" Foreground="{DynamicResource SettingsMainText}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <TextBlock Text="Select the application's language." Foreground="{DynamicResource SettingsSubText}" FontSize="14" Margin="0,0,0,15"/>

                        <ComboBox ItemsSource="{Binding AvailableLanguages}" 
                                    SelectedItem="{Binding SelectedLanguage}" 
                                     Width="300" 
                                    HorizontalAlignment="Left" 
                                    Padding="8" 
                                    FontSize="14"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Background="{DynamicResource FooterBackground}" >
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                <Button Content="Save changes" 
                        Command="{Binding SaveSettingsCommand}" 
                        Width="150" Padding="10,8" Margin="5"
                        Background="{DynamicResource SettingsSaveButtonBackground}" Foreground="{DynamicResource SettingsSaveButtonForeground}" 
                        BorderThickness="0" 
                        FontSize="16" FontWeight="SemiBold" Cursor="Hand"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
